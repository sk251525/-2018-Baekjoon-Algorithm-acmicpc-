#include <stdio.h>
#include <string.h>
int main(){
	int i, n, sum=0;
	char str[100+5];
	
	scanf("%d %s", &n, str);
	for(i = 0; i < n; i++){
		sum += str[i] - '0';
	}
	
	printf("%d", sum);
  return 0;
}

#include <stdio.h>
int main(){
    int i,in,out;
    int sum=0, total=0;
    //freopen("in.txt","r",stdin);
    for(i=0;i<10;i++){
        scanf("%d %d",&out,&in);

        sum+=in-out;
        if(total<sum) total=sum;
    }
    printf("%d\n",total);
    return 0;
}

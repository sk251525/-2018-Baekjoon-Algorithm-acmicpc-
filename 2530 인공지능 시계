#include <stdio.h>
int main(){
    int h,m,s,d;
    //freopen("in.txt","r",stdin);
    scanf("%d %d %d %d",&h,&m,&s,&d);
    printf("%d %d %d\n",(((s+d)/60+m)/60+h)%24 ,((s+d)/60+m)%60,(s+d)%60);
  
    return 0;
}
